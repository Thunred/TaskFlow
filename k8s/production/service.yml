apiVersion: v1
kind: Service
metadata:
  name: taskflow-backend
  namespace: taskflow-production
spec:
  type: NodePort
  selector:
    app: taskflow-backend
  ports:
  - name: http
    port: 3001
    targetPort: 3001
    nodePort: 30091
